/** Identifies a replacement for idempotency and hover. */
export const GLOSS_MARKER_ATTR = "data-gloss-id";

/** Original phrase for hover/options (e.g. show on hover). */
export const GLOSS_SOURCE_ATTR = "data-gloss-source";

/** Class added for static styling and viewport animation. */
export const GLOSS_WRAPPER_CLASS = "gloss-plus-one-replacement";
